# API reference

```{toctree}
:maxdepth: 1

rest
examples
auth
errors
```

The HMI bridge exposes a small HTTP API: a state snapshot, a live event stream, and
a control endpoint. The TASE.2 / ICCP object model itself is documented in
{doc}`../concepts/data-model`.
