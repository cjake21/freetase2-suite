# Installation

```{toctree}
:maxdepth: 1

requirements
local
docker
offline
troubleshooting
```

The build clones and compiles libIEC61850 (pinned) once, then compiles the tools.
After that you can run any of the demos with no hardware. Choose a local build or
the container image.
